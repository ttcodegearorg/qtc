{
  "id": "categories",
  "name": "categories",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$sales_categories",
      "name": "categories",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "replicattion-u-ap",
      "schema": "{{task.replicattion-u-ap:null$_$sales.schema}}"
    }
  ]
}